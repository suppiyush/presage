{
  "packages": {
    "python": "3.12"
  },
  "phases": {
    "install": {
      "cmds": ["pip install -r backend/requirements.txt"]
    }
  },
  "deploy": {
    "startCmd": "uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
  }
}
